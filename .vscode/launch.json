{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "create_db",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/build/Debug/bin/create_db",
            "args": [
            ],
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "Debug Build"
        }
    ]
}
